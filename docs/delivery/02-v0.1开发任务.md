# v0.1 开发任务

状态：Engineering accepted（仅待产品黄金数据集）
目标：按依赖顺序完成 `packages -> Java API -> Python AI -> Web -> 联调验收`。  
更新规则：只有某一级任务下的所有子任务完成并通过验证，才能勾选该一级任务。

## 状态说明

- `[ ]` 未完成
- `[x]` 已完成并验证
- 进行中的任务在标题后标记 `进行中`
- 阻塞任务在标题后标记 `阻塞：原因`
- 每次完成任务必须在“完成记录”中写明日期、验证命令和结果

## 总体进度

- [x] P0 Packages：契约、编辑器模型和 UI 基础包
- [x] J0 Java API：业务后端和基础设施接入
- [x] A0 Python AI：解析、脱敏、抽取与 Mock 模型
- [x] W0 Web：上传、Schema、抽取复核和模板校验页面
- [ ] I0 联调验收：工程链路已通过，仅 I9 产品黄金数据集待输入

## 依赖顺序

```text
P0 Packages
  -> J0 Java API
  -> A0 Python AI
  -> W0 Web
  -> I0 联调验收
```

Java API 完成最小上传/任务接口后，Python AI 可以与 Java 后续任务并行；Web 在 OpenAPI 和首批 API 稳定后开始。总体勾选仍按每一端全部任务完成为准。

## P0 Packages（完成）

完成条件：Monorepo JS 工具链可运行，契约是三端唯一事实源，编辑器和 UI 包具有可测试的最小导出。

### P1 工作区基础（完成）

- [x] P1.1 创建根 `package.json`、`pnpm-workspace.yaml` 和统一脚本
- [x] P1.2 固定 Node/pnpm 版本并补充安装说明
- [x] P1.3 配置 TypeScript、ESLint、Prettier 和基础测试框架
- [x] P1.4 配置 `.editorconfig` 和通用忽略文件

### P2 Contracts（完成）

- [x] P2.1 初始化 `packages/contracts` 包结构
- [x] P2.2 定义统一 ID、时间、分页、审计和错误模型
- [x] P2.3 定义原件、原件版本、上传会话契约
- [x] P2.4 定义 Schema 字段、Schema 版本和 Schema 模板契约
- [x] P2.5 定义敏感规则模板、敏感类型和令牌契约
- [x] P2.6 定义抽取任务、字段结果、证据、候选值和默认值来源契约
- [x] P2.7 定义模板、模板版本和转换告警契约
- [x] P2.8 定义任务状态、CloudEvents 事件和幂等字段
- [x] P2.9 编写 OpenAPI v1 初稿和 JSON Schema
- [x] P2.10 增加契约校验测试与 TypeScript 类型生成

### P3 Editor（完成）

- [x] P3.1 初始化 `packages/editor` 包结构
- [x] P3.2 定义受控文档模型：段落、标题、列表、表格、图片、页眉页脚、目录、分页提示
- [x] P3.3 定义模板占位符节点和字段绑定规则
- [x] P3.4 实现文档模型到安全 HTML 的序列化接口
- [x] P3.5 实现安全 HTML 到文档模型的反序列化接口
- [x] P3.6 增加 Schema 校验、HTML 白名单和单元测试

### P4 UI（完成）

- [x] P4.1 初始化 `packages/ui` 包结构
- [x] P4.2 建立颜色、字体、间距和状态设计令牌
- [x] P4.3 建立按钮、表单字段、状态提示和分栏布局基础组件
- [x] P4.4 增加无障碍和组件测试基线

## J0 Java API

完成条件：Spring Boot 服务能持久化业务对象、管理文件和任务，并通过契约测试安全调用 Python AI 服务。

### J1 服务基础（完成）

- [x] J1.1 初始化 Java 17/Spring Boot 3 工程
- [x] J1.2 配置 PostgreSQL、Redis、RabbitMQ、MinIO 客户端
- [x] J1.3 配置 Flyway、统一异常、请求 ID 和结构化日志
- [x] J1.4 配置本地环境变量和健康检查

### J2 身份与工作区（完成）

- [x] J2.1 建立用户、工作区、成员和角色模型
- [x] J2.2 实现最小登录鉴权和对象级权限校验
- [x] J2.3 实现基础审计事件

### J3 原件与上传（完成）

- [x] J3.1 建立 `source_document/source_version/source_preview/source_upload_session` 表和领域模型
- [x] J3.2 实现 DOC/DOCX/PDF、10MB、MIME、空文件和文件内容真实性校验
- [x] J3.3 实现 MinIO 上传会话、staging 到 immutable 条件复制与四类 bucket 路由
- [x] J3.4 实现服务端 SHA-256、不可变版本和创建/完成幂等处理
- [x] J3.5 实现原件列表、详情、版本创建、只读原件和预览状态接口

### J4 Schema 与规则模板（完成）

- [x] J4.1 建立 Schema、Schema 版本和字段表
- [x] J4.2 实现 Schema 表单保存、校验、版本和模板关联接口
- [x] J4.3 实现默认值规则：允许空字符串、禁止表达式、无默认值返回 `null`
- [x] J4.4 建立敏感规则模板表和管理接口

### J5 任务与抽取结果

- [x] J5.1 建立任务、抽取运行、字段结果、证据和候选值表
- [x] J5.2 实现 RabbitMQ 发布、消费、重试和死信队列
- [x] J5.3 实现 AI 服务客户端、超时、熔断和幂等回调
- [x] J5.4 实现 JSON Schema 校验、敏感令牌受控回填和结果持久化
- [x] J5.5 实现多个候选值进入人工复核
- [x] J5.6 实现任务状态 SSE 和结果审批接口

### J6 模板（完成）

- [x] J6.1 建立模板和模板版本模型
- [x] J6.2 编排 LibreOffice 预览和文档转换任务
- [x] J6.3 保存中间文档模型、安全 HTML、资源和转换告警
- [x] J6.4 实现模板版本创建、发布、回滚和原件关联接口

### J7 Java 验证

- [x] J7.1 单元测试
- [x] J7.2 PostgreSQL/Redis/RabbitMQ/MinIO 集成测试
- [x] J7.3 OpenAPI 契约测试
- [x] J7.4 权限、幂等、失败重试和敏感数据日志测试

## A0 Python AI（完成）

完成条件：FastAPI 能完成解析、敏感令牌化、Mock 抽取、验证和证据返回，并可切换第三方模型 Provider。

### A1 服务基础（完成）

- [x] A1.1 初始化 Python/FastAPI、依赖管理、格式化和测试
- [x] A1.2 使用 Pydantic 实现 contracts 对应模型
- [x] A1.3 实现健康检查、请求 ID、结构化日志和错误模型

### A2 文档解析（完成）

- [x] A2.1 实现 DOCX OOXML 解析
- [x] A2.2 实现 PDF 文本/版面解析和 OCR 扩展接口
- [x] A2.3 生成统一中间文档模型并保留段落、表格、图片、页眉页脚、目录和位置
- [x] A2.4 实现 LibreOffice headless 转换适配器和转换告警

### A3 敏感信息（完成）

- [x] A3.1 集成 Microsoft Presidio
- [x] A3.2 集成国内外手机号、邮箱和九国主要证件校验器
- [x] A3.3 实现正则、词典和自定义 Recognizer 模板
- [x] A3.4 实现稳定令牌化、重叠命中优先级和泄露检测
- [x] A3.5 明确敏感映射只在受控应用层持有，不发送给第三方模型

### A4 抽取工作流（完成）

- [x] A4.1 实现 LangChain/LangGraph Provider Adapter
- [x] A4.2 实现 Mock Provider
- [x] A4.3 实现 Schema 约束抽取、JSON 校验和证据对齐
- [x] A4.4 实现缺失值、默认值和多个候选值语义
- [x] A4.5 实现模型超时、重试、Token 预算和未知令牌拒绝

### A5 Python 验证（完成）

- [x] A5.1 解析器单元测试
- [x] A5.2 敏感信息识别与零明文泄露测试
- [x] A5.3 Mock 抽取和 JSON Schema 契约测试
- [x] A5.4 固定 Mock 文档评测脚本

## W0 Web

完成条件：用户可完成上传、Schema 配置、任务查看、结果复核和原件-模板人工校验。

### W1 应用基础（完成）

- [x] W1.1 初始化 Vue 3、TypeScript、Vite、Router、Pinia
- [x] W1.2 接入 `packages/contracts/editor/ui`
- [x] W1.3 建立 API 客户端、错误处理、鉴权和路由守卫

### W2 原件与 Schema（完成）

- [x] W2.1 原件上传、类型/大小提示和进度
- [x] W2.2 原件列表、状态、版本和只读预览
- [x] W2.3 Schema 表单、字段排序、类型和默认值校验
- [x] W2.4 Schema 模板选择、另存和版本展示
- [x] W2.5 敏感规则模板选择和管理

### W3 抽取复核（完成）

- [x] W3.1 创建抽取任务并订阅 SSE 状态
- [x] W3.2 展示 JSON、字段来源、置信度和证据
- [x] W3.3 展示 `null`、空字符串、默认值和多个候选状态
- [x] W3.4 接受、修改、驳回和审批结果

### W4 模板校验（完成）

- [x] W4.1 左侧原件只读预览
- [x] W4.2 右侧模板预览/编辑切换
- [x] W4.3 模板微调、保存新版本、发布和回滚
- [x] W4.4 展示转换告警和未支持版式

### W5 Web 验证

- [x] W5.1 组件和状态测试
- [x] W5.2 上传、抽取、复核和模板校验 E2E
- [x] W5.3 权限、错误、空状态和无障碍检查

## I0 联调验收

完成条件：v0.1 关键链路在本地和演示环境通过，并有可复现的验证记录。

- [x] I1 启动 PostgreSQL、Redis、RabbitMQ、MinIO 和应用服务
- [x] I2 跑通 DOC/DOCX/PDF 上传到 MinIO 和原件预览
- [x] I3 跑通 Schema 模板、敏感规则模板和 Mock 抽取
- [x] I4 跑通第三方模型 Provider 配置与契约，保持 Mock 为本地默认
- [x] I5 跑通字段证据、默认值、多个候选和人工审批
- [x] I6 跑通原件到 HTML 模板和左右人工校验
- [x] I7 完成日志脱敏、权限、失败重试和幂等验证
- [x] I8 补齐启动、接口、数据库迁移和故障处理文档
- [ ] I9 产品侧提供真实脱敏文档后完成黄金数据集评测

## 完成记录

| 日期 | 任务 | 结果 | 验证 |
| --- | --- | --- | --- |
| 2026-07-25 | P1.1 | 建立 pnpm 工作区和根级开发、质量、构建、测试及基础设施脚本 | `pnpm list --depth -1 --recursive`、`pnpm run check`、`pnpm --recursive --if-present run build` 通过 |
| 2026-07-25 | P1.2 | 固定 Node 24.14.1 和 pnpm 10.13.1，增加严格引擎配置、锁文件及安装/升级说明 | `pnpm install --offline --frozen-lockfile`、版本字段校验、`pnpm run check` 通过 |
| 2026-07-25 | P1.3 | 建立 TypeScript 严格基线、ESLint Flat Config、Prettier、Vitest/V8 coverage 和工作区冒烟测试 | `pnpm run check`、`pnpm run test:coverage` 通过，1 个测试通过 |
| 2026-07-25 | P1.4 | 建立跨语言 EditorConfig、Git/Docker/Prettier 通用忽略规则并保护环境样例 | `pnpm run check` 通过，2 个测试通过；`git check-ignore` 验证缓存和 coverage 被忽略、`.env.example` 可提交 |
| 2026-07-25 | P2.1 | 初始化 `@docmind/contracts` 工作区包、构建入口和 OpenAPI/JSON Schema/事件目录 | 根 `pnpm run check` 通过，3 个测试通过；包级 build/typecheck/test 和 ESM 导入验证通过 |
| 2026-07-25 | P2.2 | 定义品牌化实体 ID、RFC 3339 时间、游标分页、审计元数据和统一 API 错误模型 | 包级 lint/typecheck/test/build 通过，4 个测试通过 |
| 2026-07-25 | P2.3 | 定义 DOC/DOCX/PDF 原件、不可变原件版本、只读预览和短期签名上传会话契约 | 包级 lint/typecheck/test/build 通过，7 个测试通过 |
| 2026-07-25 | P2.4 | 定义表单字段、无表达式默认值、不可变 Schema 版本和可复用 Schema 模板契约 | 包级 lint/typecheck/test/build 通过，10 个测试通过 |
| 2026-07-25 | P2.5 | 定义 Presidio/正则/词典/校验器规则模板、敏感类型和不含原文的令牌引用 | 包级 lint/typecheck/test/build 通过，13 个测试通过 |
| 2026-07-25 | P2.6 | 定义抽取运行、字段最终值、来源、证据、置信度、候选值和人工复核契约 | 包级 lint/typecheck/test/build 通过，15 个测试通过 |
| 2026-07-26 | P2.7 | 定义模板、模板版本、安全 HTML、资源和转换告警契约 | `pnpm run typecheck`、`pnpm run test`、`pnpm run build` 通过 |
| 2026-07-26 | P2.8 | 定义 CloudEvents 形态、领域事件和幂等字段契约 | `pnpm run typecheck`、`pnpm run test`、`pnpm run build` 通过 |
| 2026-07-31 | P2.9 | 补齐 OpenAPI v1 初稿、模型抽取/领域事件/权威 Diff JSON Schema，并增加工件测试 | `pnpm run check` 通过 |
| 2026-07-31 | P2.10 | 使用 `openapi-typescript` 生成类型并通过包子路径导出，校验 OpenAPI 引用、操作名和生成类型 | `pnpm run check` 通过 |
| 2026-07-31 | P3.1-P3.3 | 初始化编辑器包，定义版本化受控文档模型、稳定节点 ID、字段占位符和数组重复块 | `pnpm run check` 通过 |
| 2026-07-31 | P3.4-P3.6 | 实现安全 HTML/CSS 往返、惰性模型元数据、白名单、深度/节点限制及结构校验 | `pnpm run check` 通过，19 个测试文件、48 项测试通过 |
| 2026-07-31 | P4.1-P4.3 | 初始化 UI 包，建立编辑出版工作台设计令牌及按钮、字段、状态、受控分栏组件 | 包级 typecheck、test、build 通过 |
| 2026-07-31 | P4.4 / P0 | 补齐 SSR、ARIA、键盘和双栏收起兜底测试，完成 Packages 阶段 | `pnpm run check` 通过，21 个测试文件、54 项测试通过 |
| 2026-07-31 | J1.1-J1.4 | 初始化 Spring Boot API，接入 PostgreSQL/Redis/RabbitMQ/MinIO/Flyway，建立统一错误、请求 ID、结构化日志及存活/就绪探针 | `pnpm run check` 通过，Java 3 个测试类、6 项测试通过 |
| 2026-07-31 | J2.1-J2.3 | 建立用户/工作区/成员/角色、短时效 JWT、密码哈希、对象级权限、创建幂等和追加式审计 | `pnpm run check` 通过，TS 22 个测试文件、56 项测试及 Java 4 个测试类、11 项测试通过 |
| 2026-07-31 | J3.1-J3.5 | 建立原件/版本/上传会话/预览模型；完成 DOC/DOCX/PDF 真实性、大小、MIME、SHA/ETag 校验；用 ETag 条件复制隔离 staging 与不可变对象；提供版本、列表、详情和只读访问 API | `pnpm run check` 通过，TS 22 个测试文件、57 项测试及 Java 6 个测试类、17 项测试通过；真实 PostgreSQL/MinIO 联调保留在 J7.2 |
| 2026-08-01 | J4.1-J4.4 | 建立 Schema/字段/模板和敏感规则不可变版本模型；完成对象级权限、幂等、嵌套 JSON Schema、字面量默认值、RE2 正则、受限词典/校验器及版本历史 API | `pnpm run check` 通过，TS 22 个测试文件、57 项测试及 Java 7 个测试类、21 项测试通过；真实 PostgreSQL Flyway 验证保留在 J7.2 |
| 2026-08-01 | J5.1 | 建立通用异步任务、抽取运行、字段结果、候选值和证据模型；完成抽取创建/查询、幂等、对象级权限及加密信封持久化边界 | `pnpm run check` 通过，TS 22 个测试文件、58 项测试及 Java 8 个测试类、24 项测试通过；RabbitMQ 编排保留在 J5.2，真实 PostgreSQL Flyway 验证保留在 J7.2 |
| 2026-08-01 | J5.2 | 建立 RabbitMQ 持久工作队列和 DLQ；实现数据库发布租约、Broker confirm、手动确认、幂等消费、指数退避、最大次数和 Worker 租约恢复 | `pnpm run check` 通过，TS 22 个测试文件、58 项测试及 Java 13 个测试类、37 项测试通过；真实 PostgreSQL/RabbitMQ 路由验证保留在 J7.2 |
| 2026-08-01 | A1.1-A1.3 | 初始化 Python 3.12/FastAPI 与 uv 锁文件；建立严格 Pydantic 契约、内部服务鉴权、健康探针、请求 ID、JSON 日志和无敏感明文错误边界；接入根级质量门禁 | `pnpm run check` 通过，TS 22 个测试文件、58 项测试，Python 10 项测试及 Java 13 个测试类、37 项测试通过 |
| 2026-08-01 | A2-A5 / A0 | 完成 DOC/DOCX/PDF 解析、九国 PII 检测与稳定令牌化、LangGraph 有限抽取工作流、Mock Provider、Schema/证据/令牌/二次 PII 校验、超时重试预算及固定评测 | `pnpm run ai:check` 通过：Ruff、60 个文件格式、mypy 47 个源文件、pytest 40 项；固定评测 3/3，通过率与 data/review/schema 指标均为 1.0 |
| 2026-08-01 | J5.3-J5.5 | 接入 Python 三段 AI API、超时与熔断；增加任务隔离的敏感令牌表、AES-GCM 信封加密、原件摘要复核、Java 独立 Schema/字段/证据/令牌/泄漏校验、受控回填、多候选复核和事务持久化 | `mvn -q verify` 通过，Java 17 个测试类、42 项测试通过；Mock 全链路验证两个手机号不会以明文进入结果、字段或令牌映射列；真实 PostgreSQL/RabbitMQ/MinIO/Python HTTP 联调保留在 J7.2/I0 |
| 2026-08-01 | J5.6 / W1-W3 | 完成鉴权 SSE、字段接受/修改/驳回、结果审批，以及 Vue 上传、Schema、抽取与复核工作台 | Web 测试、生产构建与 `mvn -q verify` 通过；SSE 使用 Authorization 请求头并在不可用时轮询降级 |
| 2026-08-01 | J6.1-J6.4 / W4 | 建立模板转换任务、加密受控模型/安全 HTML/资源/告警、不可变版本与后端 Diff；完成原件-模板可伸缩双栏、节点微调、告警定位、发布及生成新版本式回滚 | `pnpm run lint/format:check/typecheck/test/build`、`pnpm run ai:check`、`mvn -q verify` 通过；TS 26 个测试文件、66 项测试，Python 40 项，Java 20 个测试类、47 项测试通过；真实基础设施 E2E 保留在 J7.2/I0 |
| 2026-08-01 | J7.2 / J6-W4 真实联调 | 在 PostgreSQL 16、Redis、RabbitMQ、MinIO、Java API 和 Python AI 上跑通两页中文 DOCX 的上传、服务端摘要、不可变原件、PDF 预览、安全 HTML、后端 Diff、v2 发布与回滚发布 v3；增加可复现 fixture/E2E 脚本 | `pnpm e2e:templates` 通过：模板任务首次消费成功，3 个不可变版本，PDF 257525 字节，0 条转换告警；修复 PostgreSQL `CHAR/VARCHAR` 校验差异、AI 启动端口、LibreOffice 本地命令及 Java h2c multipart 兼容问题，并增加 HTTP/1.1 回归测试 |
| 2026-08-01 | W5 / I2-I8 / 第 9 步 | 完成真实浏览器上传、敏感抽取、字段接受/修改/拒绝、审批、模板微调、V2 发布、历史版本与双栏交互；将抽取和审批并入可复现 MVP E2E；补齐历史预览页数和失败码显示 | `pnpm e2e:mvp` 通过：3 个字段均恢复后复核批准、3 个模板版本、两页 PDF、0 告警；DOC/DOCX/PDF 独立 E2E 均为 2 页；浏览器无重复 ID、无未命名控件、无横向溢出；TS 28 个文件 70 项、Python 41 项、Java 25 个测试类 53 项测试通过 |
