# DocMind 仓库协作规则

## 作用域

本文件适用于整个仓库。进入具体应用工作时，还必须读取并遵守该应用目录中更具体的 `AGENTS.md`。

## 应用规则路由

- 处理 `apps/docmind-web/**` 中的需求时，必须先完整阅读 `apps/docmind-web/AGENTS.md`，再分析、修改或审查代码。
- 如果当前工具没有自动加载嵌套 `AGENTS.md`，必须手动打开对应文件；不得因工具差异跳过应用级规则。
- 前端规则只约束 `apps/docmind-web/**`，不得套用到 Java Server 或 Document AI 应用。
- 处理其他应用前，先阅读对应应用的 `README.md`、构建文件和现有目录约定，不得假设不同语言栈采用相同规范。

## 通用改动原则

- 只修改完成当前任务所必需的文件，保留用户已有改动，不处理无关技术债。
- 先理解现有边界、调用链和测试，再实施变更；不得通过复制代码绕过既有模块。
- 结构迁移与功能变更尽量分开。移动文件时优先保持行为不变，并同步更新导入、路由、测试和文档。
- 不得提交密钥、令牌、真实敏感数据、内部对象存储地址或本地环境文件。
- 未经明确授权，不新增生产依赖、不更换框架、不改变公共 API 或跨应用契约。
- 仅修改文档时，不运行与文档无关的完整构建；应检查链接、格式和 `git diff`。修改代码时，执行应用级 `AGENTS.md` 要求的验证命令。

