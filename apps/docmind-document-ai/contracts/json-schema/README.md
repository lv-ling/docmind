# 模型输出 Schema

[`model-extraction-output.schema.json`](model-extraction-output.schema.json) 描述令牌化模型抽取输出。通过该 Schema 后仍必须执行二次 PII 扫描、证据校验和用户 Schema 校验。

本 Schema 属于 Document AI 提供的内部契约，修改时需要明确版本与 Server 兼容性。
